package query;

import conexao.Conexao;
import dataBase.*;
import tabelas.*;

public class Query {
	/*public void construirConexao(String user, String senha, String caminho, int porta) {
		Conexao.setUsuario(user);
		Conexao.setSenha(senha);
		Conexao.setCaminho(caminho);
		Conexao.setPorta(porta);
	}
	
	public void construirSchema(String nomeSchema) {
		DataBaseFactory db = new DataBaseFactory();
		db.removerSchema(nomeSchema);
		db.criarSchema(nomeSchema);
	}
	
	public void construirTabela(String nomeTabela, String nomeAttr, String tipoAttr, int sizeAttr) {
		TableFactory tabela = new TableFactory();
		tabela.criarTabela(nomeTabela, nomeAttr, tipoAttr, sizeAttr);
	}
	
	public void construirColuna(String nomeTabela, String nomeAttr, String tipoAttr, int sizeAttr) {
		TableFactory tabela = new TableFactory();
		tabela.criarColuna(nomeTabela, nomeAttr, tipoAttr, sizeAttr);
	}
	
	public void construirPK(String nomeAttr, String nomeTabela) {
		TableFactory tabela = new TableFactory();
		tabela.criarPK(nomeAttr, nomeTabela);
	}
	
	public void construirFK(String nome, String nomeRef, String tabelaRef, String tabela) {
		TableFactory table = new TableFactory();
		table.criarFK(nome, nomeRef, tabelaRef, tabela);
	}*/
}
